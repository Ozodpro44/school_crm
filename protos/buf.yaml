version: v1
name: buf.build/school-crm/protos
deps:
  - buf.build/googleapis/googleapis   # for google.protobuf.Timestamp etc.
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_VERSION_SUFFIX   # allow v1 suffix in package names
